class Flashlight {
    status ;
    constructor(battey, energy ) {
        this.isOn = false;
    }
        this.energy = energy;
    }

    this.getEnergy = function () {
        return this.energy;
    };

    getStatus() {
    return this.status = true;
}

    return this.status = false;
}
        return this.isOn ? 'on' : 'off'
    }
}