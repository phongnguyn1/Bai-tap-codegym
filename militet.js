inputWidth = prompt("Enter the width");
let width = parseInt(inputWidth);
let area = width * width;
let parameter = width + width + width + width
document.write("The area is: " + area);
document.write("The parameter is: " + parameter);
